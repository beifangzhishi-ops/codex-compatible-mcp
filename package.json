{
  "name": "codex-compatible-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex-inspired MCP execution harness with remote workers, sandboxed process sessions, patching, and image reads.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:native": "node scripts/build-native.mjs",
    "prestart": "npm run build:native",
    "start": "node src/index.mjs",
    "preworker": "npm run build:native",
    "worker": "node src/worker/agent.mjs",
    "pretest": "npm run build:native",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
